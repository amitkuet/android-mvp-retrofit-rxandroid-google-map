package app.engine.android.util;

import android.content.Context;
import java.util.ArrayList;

import app.engine.android.R;

public class MenuItemList {
    public ArrayList<MenuItem> getMenuList(Context context) {
        ArrayList<MenuItem> menuItemList = new ArrayList<MenuItem>();

        //MenuItem menuItem = new MenuItem();
        //menuItem.setTitle(context.getResources().getString(R.string.nav_dashboard));
        //menuItem.setActivityClass(activity.class);
        //menuItem.setImageResId(R.mipmap.icon_book_a_flight);
        //menuItemList.add(menuItem);

        return menuItemList;
    }
}
